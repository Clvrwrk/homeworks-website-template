# Project Handoff — Homeworks Website Builder
**Project:** homeworks-website-template
**Repo:** https://github.com/Clvrwrk/homeworks-website-template (not yet pushed)
**Production URL:** not yet deployed
**Date:** 2026-03-30 
**Agent:** Lead Orchestrator
**Reason:** User-requested end-of-session handoff

---

## Accomplished This Session

### Design + Planning (completed before implementation)

- `docs/superpowers/specs/2026-03-30-homeworks-website-builder-design.md`: Full system design spec — 11 sections covering standard pages, file structure, typography rules, brand color system, contact form flow, Rankability content generation, Supabase schema, Agent D frontend guardrails, 4-phase /build-website skill orchestration, env vars, future Option A webhook plan
- `docs/superpowers/plans/2026-03-30-template-repo.md`: 16-task implementation plan for homeworks-website-template repo
- `docs/superpowers/plans/2026-03-30-build-website-skill.md`: 1-task plan for writing ~/.claude/skills/build-website/SKILL.md
- `docs/superpowers/plans/2026-03-30-hawc-execution.md`: 8-task plan for HAWC General Contracting first client build (pre-flight values, WCAG analysis, populated site.ts)
- `docs/superpowers/specs/session-handoff-system.md`: Session handoff protocol reference doc (new this session)

### Skills Updated

- `~/.claude/skills/project-handoff/SKILL.md`: Updated to generate OmniDrop-style handoff format with Accomplished This Session, Git State, Task Cut Off, Next Task with copy-pasteable prompt, Decisions, Blockers, Verification Commands, Full Context. Added homeworks-website-template as recognized project.

### Template Repo — Task 1 complete

- `/Users/chussey/Documents/Claude Projects/homeworks-website-template/` (entire repo): Copied from McKinney Fixit Pros, git reinitialized, package.json updated with astro-icon@1.1.5, @iconify-json/lucide, @iconify-json/heroicons, framer-motion@11, gsap@3.14.2. `npm install` succeeded. Build passes clean. Initial commit `d7a09ec`.

## Git State
- **Branch:** main (local only — not yet pushed to GitHub)
- **Last commit:** `d7a09ec` — "feat: initialize homeworks-website-template from McKinney Fixit Pros"
- **Uncommitted changes:** none

## Task Cut Off
Task 1 of 16 complete. Stopped at a clean boundary before starting Task 2 (Replace site.ts with anonymized template config).

## Next Task — Start Here

**Task:** Task 2 — Replace site.ts with anonymized template config

**What to check / do:**
1. `cd "/Users/chussey/Documents/Claude Projects/homeworks-website-template"`
2. Overwrite `src/config/site.ts` with the full template version from the plan (all `{{TEMPLATE_VAR}}` placeholders, ServiceDef/LocationDef types, brand colors, GHL constants, SEO defaults)
3. Run `npx tsc --noEmit 2>&1 | grep "site.ts"` — expect no errors in site.ts itself
4. Commit: `git commit -m "feat: anonymize site.ts — all McKinney values replaced with template vars"`
5. Continue through Tasks 3–16 sequentially using subagent-driven-development

**If TypeScript errors appear in site.ts:** The types are self-contained — any errors are likely syntax issues in the file. Read the error and fix inline before committing.

**Prompt to use:** "Read docs/handoffs/current.md. We're building homeworks-website-template using subagent-driven-development. Task 1 is complete (commit d7a09ec). Start Task 2: replace src/config/site.ts with the anonymized template config from docs/superpowers/plans/2026-03-30-template-repo.md."

## Decisions Made This Session

- **Contact form uses Supabase→GHL (Option C), NOT GHL-only:** The original McKinney site goes directly to GHL. We chose Option C (Supabase insert first, then fire-and-forget GHL sync) to match the onboarding app pattern and give us a database record + sync status tracking.
- **Content stored in TypeScript data files (src/data/):** Rankability-generated content is stored as `Record<slug, ServiceContent>` TypeScript maps, not in a CMS or database. Pros: no runtime DB query for content, instant page loads, type-safe. The /build-website skill overwrites these files on each client build.
- **LOCAL fonts only (public/fonts/):** Switched from Google Fonts API to local .woff2 files to avoid GDPR issues and eliminate external font requests. Plus Jakarta Sans (display) + DM Sans (body). Max 2 fonts — hard rule.
- **`PUBLIC_SUPABASE_URL` (not `SUPABASE_URL`):** The existing supabase.ts uses `PUBLIC_SUPABASE_URL`. The URL is not a secret and is safe as PUBLIC_. Only the service_role key stays server-side.
- **Primary brand color #ec5b13 FAILS WCAG AA:** HAWC's orange is only 3.37:1 with white text (needs 4.5:1). Adjusted to #b8470e (4.95:1 passes). This is documented in Plan C (hawc-execution.md) Task 5.
- **Agent D frontend guardrails are explicit rules, not suggestions:** Max 1.03 scale on pulse animations, GSAP ScrollTrigger with `once: true` (no looping), no `transform` on content containers (breaks layout), WCAG AA required before any color ships.

## Blockers Requiring Human Action

1. **GitHub repo not yet created or pushed** — Run Task 16 when template is complete. Requires GITHUB_PERSONAL_ACCESS_TOKEN with `repo` + `org:read` scopes on Clvrwrk org.
2. **Font files not yet downloaded** — Task 7 downloads Plus Jakarta Sans + DM Sans .woff2 files from Google Fonts static URLs to `public/fonts/`. If URLs have rotated, use `npx fontsource download dm-sans plus-jakarta-sans` as fallback.
3. **Rankability API key needed for Plan C (HAWC build)** — RANKABILITY_API_KEY must be set before running the /build-website skill for HAWC. Get from rankability.com/developers.

## Verification Commands
1. `cd "/Users/chussey/Documents/Claude Projects/homeworks-website-template" && git log --oneline -1` — should return `d7a09ec feat: initialize homeworks-website-template from McKinney Fixit Pros`
2. `ls "/Users/chussey/Documents/Claude Projects/homeworks-website-template/node_modules/astro-icon"` — should show package contents
3. `ls "/Users/chussey/Documents/Claude Projects/homeworks-website-template/public/fonts"` — should be empty (fonts downloaded in Task 7)

## Full Context

### What was built across ALL sessions (complete feature list)

**Session 1 (2026-03-30):**
- Cloned McKinney Fixit Pros as starting template
- Full brainstorming + design for Homeworks Website Builder system
- Design spec (11 sections), 3 implementation plans (template repo, build-website skill, HAWC execution)
- Updated project-handoff skill to OmniDrop format
- Added session-handoff-system reference doc
- Task 1 of 16: homeworks-website-template repo initialized with new dependencies

### Architecture decisions

- **Single source of truth:** `src/config/site.ts` holds ALL client-specific constants as `{{TEMPLATE_VAR}}` strings. The /build-website skill does a single-pass replacement of this file. Nothing else in the codebase is client-specific.
- **Brand color injection:** Layout.astro uses Astro's `define:vars` to set `:root` CSS custom properties at build time. `global.css` consumes these via `var(--brand-primary)` etc. No CSS-in-JS, no runtime theming.
- **4-agent parallel Phase 1:** When /build-website runs, Agent A (GitHub), Agent B (Supabase), Agent C (Rankability content), Agent D (WCAG/design review) run in parallel. Phase 2 (populate + push files) waits for all four.
- **Contact sync is fire-and-forget:** `/api/contact` inserts to Supabase and returns 303 redirect immediately. GHL sync happens async in `/api/contact-sync`. If sync fails 3 times, status becomes `needs_attention` and an alert email fires via GHL.
- **Rankability content is 24 jobs for HAWC:** 15 service pages + 9 location pages, all parallel, poll every 10s, max 10 min timeout. Results stored as TypeScript data files.

### Design system

- **Fonts:** Plus Jakarta Sans (display/headings, `--font-display`) + DM Sans (body, `--font-sans`). Two fonts max — never add a third.
- **Type scale:** Golden ratio, base 17px, ratio 1.618. h1: clamp(2.75rem, 5.5vw, 4.5rem). h2: clamp(2rem, 3.75vw, 2.875rem).
- **Colors:** All via CSS vars — `--brand-primary` (CTA fill), `--brand-secondary` (nav/dark sections), `--brand-accent` (light section bg), `--brand-accent-2` (body text). Fixed neutrals: `--color-ink: #151d25`, `--color-muted: #567798`.
- **Animation:** GSAP ScrollTrigger for `.reveal-up` (opacity 0→1, y 24→0, 400ms, once:true). CSS-only `icon-enter` keyframe for icons. `cta-pulse-mobile` pulse (max scale 1.03, 2s, mobile only).
- **Buttons:** `.btn-primary`, `.btn-secondary`, `.btn-navy`, `.btn-ghost`, `.btn-submit` — all min-height 54px, font-display, 700 weight, border-radius 9999px.

### Key invariants (never violate)

- `SUPABASE_SERVICE_ROLE_KEY` is never `PUBLIC_SUPABASE_SERVICE_ROLE_KEY` — server-side only
- `GHL_API_KEY` is never `PUBLIC_GHL_API_KEY` — server-side only
- `RANKABILITY_API_KEY` is never `PUBLIC_RANKABILITY_API_KEY` — server-side only
- `PUBLIC_SUPABASE_URL` is correct (URL is not a secret)
- WCAG AA minimum 4.5:1 for normal text, 3:1 for large text/UI components — validate before shipping any brand color
- Max 2 fonts — Plus Jakarta Sans + DM Sans. Never add a third.
- Max animation scale: 1.03. No looping animations on page content. `once: true` on all ScrollTriggers.
- Contact form always writes to Supabase first, then fires GHL sync async. Never write directly to GHL only.

### Service / deployment map

| Service | Detail |
|---------|--------|
| GitHub org | Clvrwrk — all client repos created here as private repos |
| Supabase | One project per client — provisioned via MCP during /build-website |
| GoHighLevel | Per-client location ID, pipeline ID, stage ID — set in site.ts |
| Rankability | RANKABILITY_API_KEY env var — one key, multiple client_ids |
| Coolify | nixpacks deployment — port 4321, `node ./dist/server/entry.mjs` start command |
| Plans location | `/Users/chussey/Documents/Claude Projects/docs/superpowers/plans/` |
| Specs location | `/Users/chussey/Documents/Claude Projects/docs/superpowers/specs/` |
